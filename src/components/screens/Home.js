import { View, Text, SafeAreaView } from 'react-native'
import React from 'react'

const Home = (navigation) => {
    return (
        <SafeAreaView className='flex'>
            <View className='flex'>
                <Text className='font-[UnboundedRegular]'>YM GALLERY</Text>
            </View>
        </SafeAreaView>
    )
}

export default Home